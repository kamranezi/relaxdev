name: Deploy Dashboard to Yandex

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # 1. Логин в Docker Яндекса
      - name: Login to Yandex CR
        uses: docker/login-action@v3
        with:
          registry: cr.yandex
          username: json_key
          password: ${{ secrets.YC_SA_KEY_JSON }}

      # 2. Установка YC CLI
      - name: Install YC CLI
        run: |
          curl -sSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash
          sudo ln -s /home/runner/yandex-cloud/bin/yc /usr/bin/yc

      # 3. Настройка профиля YC
      - name: Authenticate YC CLI
        env:
          YC_KEY: ${{ secrets.YC_SA_KEY_JSON }}
        run: |
          echo "$YC_KEY" > key.json
          yc config profile create sa-profile
          yc config set service-account-key key.json
          yc config set folder-id ${{ secrets.YC_FOLDER_ID }}
          rm -f key.json

      # 4. Сборка и отправка образа (ИСПРАВЛЕНО)
      - name: Build and Push
        env:
          # Устанавливаем правильный URL для сборки
          NEXTAUTH_URL: https://relaxdev.ru
          NEXT_PUBLIC_API_URL: https://relaxdev.ru
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_CLIENT_EMAIL: ${{ secrets.FIREBASE_CLIENT_EMAIL }}
          FIREBASE_PRIVATE_KEY: ${{ secrets.FIREBASE_PRIVATE_KEY }}
          GITHUB_ID: ${{ secrets.GH_OAUTH_ID }}
          GITHUB_SECRET: ${{ secrets.GH_OAUTH_SECRET }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
        run: |
          IMAGE_URI="cr.yandex/${{ secrets.YC_REGISTRY_ID }}/ruvercel-dashboard:${{ github.sha }}"
          
          echo "🔨 Building Docker image..."
          docker build \
            --progress=plain \
            --build-arg NEXTAUTH_URL \
            --build-arg NEXT_PUBLIC_API_URL \
            --build-arg FIREBASE_PROJECT_ID \
            --build-arg FIREBASE_CLIENT_EMAIL \
            --build-arg FIREBASE_PRIVATE_KEY \
            --build-arg GITHUB_ID \
            --build-arg GITHUB_SECRET \
            --build-arg GOOGLE_CLIENT_ID \
            --build-arg GOOGLE_CLIENT_SECRET \
            --build-arg NEXTAUTH_SECRET \
            -t $IMAGE_URI . 2>&1 | tee build.log
          
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "❌ Build failed! Last 50 lines:"
            tail -50 build.log
            exit 1
          fi
          
          echo "📦 Pushing image to registry..."
          docker push $IMAGE_URI
          
          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV
          echo "✅ Image pushed: $IMAGE_URI"

      # 5. Обновление контейнера (ИСПРАВЛЕНО)
      - name: Deploy Revision
        env:
          FIREBASE_PRIVATE_KEY_SECRET: ${{ secrets.FIREBASE_PRIVATE_KEY }}
        run: |
          echo "🚀 Deploying new revision..."
          
          yc serverless container revision deploy \
            --container-name ruvercel-frontend \
            --folder-id ${{ secrets.YC_FOLDER_ID }} \
            --image ${{ env.IMAGE_URI }} \
            --service-account-id ${{ secrets.YC_SA_ID }} \
            --cores 1 \
            --memory 512M \
            --execution-timeout 30s \
            --environment FIREBASE_PROJECT_ID=${{ secrets.FIREBASE_PROJECT_ID }} \
            --environment FIREBASE_CLIENT_EMAIL=${{ secrets.FIREBASE_CLIENT_EMAIL }} \
            --environment FIREBASE_PRIVATE_KEY="${FIREBASE_PRIVATE_KEY_SECRET}" \
            --environment GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }} \
            --environment GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }} \
            --environment YC_FOLDER_ID=${{ secrets.YC_FOLDER_ID }} \
            --environment YC_REGISTRY_ID=${{ secrets.YC_REGISTRY_ID }} \
            --environment YC_SA_ID=${{ secrets.YC_SA_ID }} \
            --environment GITHUB_ACCESS_TOKEN=${{ secrets.GH_PAT }} \
            --environment BUILDER_REPO_OWNER=kamranezi \
            --environment BUILDER_REPO_NAME=ruvercel-builder \
            --environment GITHUB_ID=${{ secrets.GH_OAUTH_ID }} \
            --environment GITHUB_SECRET=${{ secrets.GH_OAUTH_SECRET }} \
            --environment NEXTAUTH_SECRET=${{ secrets.NEXTAUTH_SECRET }} \
            # Устанавливаем правильный URL для runtime
            --environment NEXTAUTH_URL=https://relaxdev.ru \
            --environment NODE_ENV=production
          
          echo "✅ Deployment complete!"

      # 6. Очистка
      - name: Cleanup
        if: always()
        run: |
          rm -f key.json
          docker system prune -f