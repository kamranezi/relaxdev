{
  "rules": {
    "admins": {
      ".read": "auth != null",
      ".write": "root.child('admins').child(auth.uid).val() === true"
    },
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('admins').child(auth.uid).val() === true)",
        ".write": "auth != null && (auth.uid === $uid || root.child('admins').child(auth.uid).val() === true)"
      }
    },
    "projects": {
      ".read": "auth != null",
       ".write": "root.child('admins').child(auth.uid).val() === true",
       "$projectId": {
        ".read": "data.child('isPublic').val() === true || (auth != null && (root.child('admins').child(auth.uid).val() === true || data.child('owner').val() === auth.token.email))",
         ".write": "auth != null && (root.child('admins').child(auth.uid).val() === true || (newData.child('owner').val() === auth.token.email && data.child('owner').val() === auth.token.email))"
       }
    }
  }
}
