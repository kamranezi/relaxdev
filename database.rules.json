{
  "rules": {
    "admins": {
      ".read": "auth != null",
      ".write": "root.child('admins').child(auth.uid).val() === true"
    },
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('admins').child(auth.uid).val() === true)",
        ".write": "auth != null && (auth.uid === $uid || root.child('admins').child(auth.uid).val() === true)"
      }
    },
    "projects": {
      ".indexOn": ["owner", "isPublic"],
      ".read": "(query.orderByChild === 'isPublic' && query.equalTo === true) || (auth != null && ((query.orderByChild === 'owner' && query.equalTo === auth.token.email) || root.child('admins').child(auth.uid).val() === true))",
      ".write": "root.child('admins').child(auth.uid).val() === true",
      "$projectId": {
        ".read": "data.child('isPublic').val() === true || (auth != null && (data.child('owner').val() === auth.token.email || root.child('admins').child(auth.uid).val() === true))",
        ".write": "auth != null && (root.child('admins').child(auth.uid).val() === true || (newData.child('owner').val() === auth.token.email && data.child('owner').val() === auth.token.email))"
      }
    }
  }
}